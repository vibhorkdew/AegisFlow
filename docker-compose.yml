version: '3.9'
services:
  aegisflow: 
    build: .
    container_name: aegisflow_container

    ports:
      - "7000:7000"
    environment:
      - FLASK_ENV=development

    volumes:
      - .:/app

    restart: always


